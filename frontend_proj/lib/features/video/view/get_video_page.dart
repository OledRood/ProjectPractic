import 'package:flutter/material.dart';

class GetVideoPage extends StatelessWidget {
  const GetVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Get Video Page')));
  }
}

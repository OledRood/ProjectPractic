# 🎥 Система обработки видео с упражнениями

Полная инструкция по запуску и использованию системы клиент-сервер для обработки видео с фитнес-упражнениями.

## 📋 Оглавление

1. [Архитектура системы](#архитектура-системы)
2. [Запуск Backend](#запуск-backend)
3. [Запуск Frontend](#запуск-frontend)
4. [Режим заглушек](#режим-заглушек)
5. [Интеграция с ML моделью](#интеграция-с-ml-моделью)
6. [Тестирование](#тестирование)

## 🏗️ Архитектура системы

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│  Flutter App    │  HTTP   │  Python Backend │  Local  │   ML Model      │
│  (Dart/Dio)     │◄───────►│  (FastAPI)      │◄───────►│  (Python)       │
└─────────────────┘         └─────────────────┘         └─────────────────┘
```

### Endpoints:
- `POST /api/upload` - загрузка видео (любой формат)
- `GET /api/status/{task_id}` - проверка статуса
- `GET /api/result/{task_id}` - получение результата (всегда MP4)
- `GET /api/health` - проверка работоспособности

### Особенности:
- ✅ **FastAPI** - современный асинхронный фреймворк
- ✅ **Автоматическая документация** - Swagger UI и ReDoc
- ✅ **Последовательная обработка** - только одно видео обрабатывается одновременно
- ✅ **Система очередей** - новые задачи ставятся в очередь
- ✅ **Любой формат видео на входе** - mp4, avi, mov, mkv, webm, flv, wmv
- ✅ **MP4 на выходе** - унифицированный формат результата
- ✅ **Заглушки ML модели** - для разработки без реальной обработки
- ✅ **Реалтайм статус** - опрос статуса каждые 2 секунды
- ✅ **Task ID** - уникальный идентификатор для каждой задачи

## 🚀 Запуск Backend

### Шаг 1: Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### Шаг 2: Запуск сервера

```bash
python app.py
```

Сервер будет доступен по адресу: `http://localhost:5000`

### Шаг 3: Проверка работоспособности

Откройте браузер и перейдите по адресу:
- **API**: `http://localhost:8000/api/health`
- **Swagger UI**: `http://localhost:8000/docs` (интерактивная документация)
- **ReDoc**: `http://localhost:8000/redoc` (альтернативная документация)

Или через curl:
```bash
curl http://localhost:8000/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "message": "Server is running",
  "processing": false
}
```

## 📱 Запуск Frontend

### Шаг 1: Убедитесь что все зависимости установлены

```bash
cd frontend_proj
flutter pub get
```

### Шаг 2: Настройка подключения к серверу

Откройте `lib/core/video/services/video_api_service.dart` и измените `BASE_URL`:

```dart
// Для эмулятора Android
const String BASE_URL = 'http://10.0.2.2:8000/api';

// Для симулятора iOS или Web
const String BASE_URL = 'http://localhost:8000/api';

// Для реального устройства
const String BASE_URL = 'http://YOUR_COMPUTER_IP:8000/api';
```

### Шаг 3: Запуск приложения

```bash
flutter run
```

### Шаг 4: Открытие тестового экрана

Добавьте в навигацию или используйте напрямую:

```dart
import 'package:frontend_proj/features/video/screens/video_processing_example_screen.dart';

// Навигация к экрану
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => VideoProcessingExampleScreen(),
  ),
);
```

## 🔧 Режим заглушек

### Backend (Python)

**Текущее состояние:** Активирован режим заглушек

В файле `backend/app.py` функция `process_video_task` использует mock-данные:

```python
# 🔧 ЗАГЛУШКА: Имитация работы модели
logger.info(f"[MOCK] Обработка видео: {video_path}")

# Симулируем длительную обработку (5-10 секунд)
import random
processing_time = random.uniform(5, 10)
time.sleep(processing_time)

# 🔧 ЗАГЛУШКА: Генерируем mock результаты
mock_results = {
    'exercise_type': random.choice(['push_up', 'squat', 'long_jump']),
    'correctness': random.choice(['correct', 'incorrect', 'partial']),
    'confidence': round(random.uniform(0.7, 0.99), 2),
    'frame_count': random.randint(100, 300),
    'output_video': f'result_{task_id}.mp4'
}
```

**Что происходит:**
1. Видео загружается на сервер
2. Имитируется обработка 5-10 секунд
3. Генерируются случайные результаты
4. Создается пустой файл результата

## 🤖 Интеграция с ML моделью

### Для активации реальной обработки:

#### 1. В файле `backend/app.py`

**Удалите блоки с меткой** `🔧 ЗАГЛУШКА`

**Раскомментируйте блок:**
```python
# 📝 В продакшене раскомментировать:
from model.project_root.src.backend_interface import (
    process_video_with_rotation, 
    analyze_video_frames
)

# Обрабатываем видео и получаем кадры
frames_dir = process_video_with_rotation(video_path, rotation)

# Анализируем кадры
results = analyze_video_frames(frames_dir, fps=30)

# Копируем результирующее видео в папку results
output_video_src = Path(results['output_video'])
output_video_dst = RESULTS_FOLDER / f'result_{task_id}.mp4'
shutil.copy(output_video_src, output_video_dst)

results['output_video'] = output_video_dst.name
```

#### 2. Добавьте импорт

В начале файла `backend/app.py`:
```python
import shutil
```

#### 3. Установите зависимости для ML модели

Перейдите в директорию модели и установите зависимости:
```bash
cd ../model/project_root
pip install -r requirements.txt  # если есть
```

## 🧪 Тестирование

### 1. Тестирование Backend с curl

**Загрузка видео:**
```bash
curl -X POST \
  -F "file=@/path/to/video.mp4" \
  http://localhost:5000/api/upload
```

Ответ:
```json
{
  "task_id": "abc-123-def-456",
  "status": "queued",
  "message": "Video uploaded successfully"
}
```

**Проверка статуса:**
```bash
curl http://localhost:5000/api/status/abc-123-def-456
```

**Скачивание результата:**
```bash
curl -O http://localhost:5000/api/result/abc-123-def-456
```

### 2. Тестирование Frontend

1. Запустите тестовый экран `VideoProcessingExampleScreen`
2. Нажмите "Выбрать видео" и выберите файл
3. Нажмите "Загрузить и обработать"
4. Наблюдайте за прогрессом:
   - Загрузка видео (прогресс бар)
   - Статус: "В очереди" → "Обрабатывается" → "Завершено"
5. При завершении появится диалог с результатами
6. Нажмите "Скачать результат" для сохранения

### 3. Тестирование очереди

Попробуйте загрузить несколько видео одновременно:
- Первое видео начнет обрабатываться сразу
- Остальные будут в статусе "В очереди"
- По завершении первого, начнется обработка следующего

## 📊 Структура проекта

```
ProjectPractic/
├── backend/
│   ├── app.py                 # Flask API сервер
│   ├── requirements.txt       # Python зависимости
│   ├── uploads/              # Загруженные видео (создается автоматически)
│   └── results/              # Обработанные видео (создается автоматически)
│
├── frontend_proj/
│   └── lib/
│       ├── core/
│       │   └── video/
│       │       ├── models/
│       │       │   └── video_task.dart
│       │       ├── services/
│       │       │   └── video_api_service.dart
│       │       ├── video_di.dart
│       │       └── README.md
│       └── features/
│           └── video/
│               └── screens/
│                   └── video_processing_example_screen.dart
│
└── model/                     # ML модель (существующая)
    └── project_root/
        └── src/
            ├── backend_interface.py
            ├── fitness_pipeline_manager.py
            └── ...
```

## 🔍 Troubleshooting

### Backend не запускается

**Проблема:** `ModuleNotFoundError: No module named 'flask'`

**Решение:**
```bash
pip install -r requirements.txt
```

### Frontend не может подключиться к серверу

**Проблема:** `SocketException: Connection refused`

**Решение:**
1. Убедитесь что backend запущен (`python app.py`)
2. Проверьте `BASE_URL` в `video_api_service.dart`
3. Для Android используйте `http://10.0.2.2:5000/api`

### Видео не обрабатывается

**Проблема:** Задача висит в статусе "processing"

**Решение:**
1. Проверьте логи backend (консоль где запущен `app.py`)
2. Убедитесь что ML модель настроена корректно
3. В режиме заглушек задача должна завершиться через 5-10 секунд

## 📝 Дополнительная информация

- **Backend README:** `backend/readme.md`
- **Frontend API README:** `frontend_proj/lib/core/video/README.md`
- **ML Model:** См. `model/readme.md`

## 🎯 Следующие шаги

1. ✅ Backend с заглушками - **ГОТОВО**
2. ✅ Frontend API клиент - **ГОТОВО**
3. ✅ Тестовый экран - **ГОТОВО**
4. ⏳ Интеграция с реальной ML моделью - **TODO**
5. ⏳ Оптимизация производительности - **TODO**
6. ⏳ Добавление базы данных для хранения задач - **TODO**
7. ⏳ Деплой на production сервер - **TODO**
